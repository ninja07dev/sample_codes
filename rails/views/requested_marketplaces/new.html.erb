<div data-controller='requestedMarketplace'>
  <div class="animated fadeIn">
  <div class="row">
    <div class="col-lg-12">
      <div class="card">
        <div class="card-header"><%= t('requested_marketplace.add') %></div>
        <div class="card-body card-block">
          <div class="input-group">
            <%= form_with model: @resource, id: 'requested_marketplace', local: true do |f| %>
              <div class="form-group">
                <%= f.label :name %><br />
                <%= f.phone_field :name, autofocus: true, class: 'form-control', required: true, autocomplete: "Phone number" %>
              </div>
              <div class="actions">
                <%= f.submit t('requested_marketplace.add'), class: 'btn btn-success btn-sm' %>
              </div>
            <% end %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
</div>
